import React from 'react';
import TodoList from '../TodoList/TodoList'; // Make sure TodoList exists and is exported

export default function Index() {
  return (
    <div>
      <TodoList />
    </div>
  );
}
